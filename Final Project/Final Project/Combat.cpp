#include "Combat.h"

Combat::Combat(Actor player, Actor Enemy)
	:m_player(player), m_enemy(Enemy)
{
	startCombat();
}

void Combat::startCombat()
{
	// Implement combat logic here, e.g. turn-based attacks until one actor is defeated

	
}